---
import { Icon } from "astro-icon/components"
import Link from "../ui/Link.astro"
import Section from "./Section.astro"
---
<Section paddingYDefault>
    <div class="flex flex-col gap-4">
        <h3 class="titleSection">Have idea for web, or just say hello?</h3>
        <div class="flex gap-2">
            <Link href="/" variant="default" size="lg">
                <Icon name={'copy'} size={22} />
                E-Mail
            </Link>
            <Link target="_blank" href="https://wa.me/6289519469524" variant="default" size="lg">
                <Icon name={'whatsapp'} size={22} />
                WhatsApp
            </Link>
        </div>
        <div class="flex gap-2 items-center">
            <Link target="_blank" href="/" variant="outline" size="icon">
                <Icon name={'instagram'} size={22} />
            </Link>
            <Link target="_blank" href="/" variant="outline" size="icon">
                <Icon name={'github'} size={22} />
            </Link>
        </div>
        <code class="text-zinc-400 mt-6 text-sm">Last update — 3 September {new Date().getFullYear()}</code>
    </div>
</Section>